# Pytorch課題2

## 目的  
実際によく使われるモデルを Pytorch 上で動かせるようにすることを目的とします。  

## タスク  
VGG16、ResNet18 を使用した画像分類タスクを実施します。

## 課題  
以下の実験を行い、結果や考察をまとめます。ハイパーパラメータ等の詳細な設定は自分で決定してください。

### 1. VGG16 と ResNet18 のベンチマーク  
1. **モデルの実装**  
   - VGG16 と ResNet18 の実装を行う。  
   - 実装前にそれぞれのモデルについて調査する。  

2. **性能比較**  
   - scratch から実装したモデルと公式から取得したモデルの性能を比較する。  
   - 次の 2 つのデータセットを使用する：  
     - CIFAR-100  
     - LUVT（データセットの説明は下部に記載）

3. **学習過程の可視化**  
   - TensorBoard や WandB などを使用して学習過程をモニタリングする。  

4. **評価指標**  
   - 以下の評価指標を理解し、評価を行う。  
     - 正解率 (Accuracy)  
     - 適合率 (Precision)  
     - 再現率 (Recall)  
     - F1値 (F1 Score)  
     - 特異度 (Specificity)  

5. **推論時間の計測**  
   - GPU と CPU の両方で推論時間を比較する。  
   - FPS (Frame Per Second) を算出する。  
   - GPU を使用して計算時間を測定する際には注意が必要。

### 2. モデル構造の変更  
以下のようにモデル構造を変更し、認識性能の変化を比較実験する。その他の変更も可。

1. **活性化関数の変更**  
   - ReLU を GERU や Swish に置き換える。  

2. **Normalization の変更**  
   - Batch Normalization 以外の Normalization に置き換える。  

3. **Optimizer の変更**  
   - Optimizer を変更して比較する。  

4. **スケジューラの変更**  
   - 学習率スケジューラを変更する。  

5. **畳み込み層の変更**  
   - MobileNetV1 のように、通常の畳み込みを異なる畳み込みに置き換える。  

## データセット説明  
- **CIFAR-100**：100 クラスの画像分類データセット。  

